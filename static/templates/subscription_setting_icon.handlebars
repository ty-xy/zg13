<div class="icon" style="background-color: {{color}}">
    <div class="flex">
        {{#if invite_only}}
        <i class="icon-vector-lock"></i>
        {{else}}
        <span class="icon-vector-circle">#</span>
        {{/if}}
    </div>
</div>
